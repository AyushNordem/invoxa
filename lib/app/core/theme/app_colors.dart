import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primary = Color(0xFFFF6B00);
  static const Color primaryLight = Color(0xFFFFE2CC);
  static const Color primaryLight2 = Color(0xFFFFD1AD);
  static const Color primaryLight3 = Color(0xFFFFC08F);
  static const Color primarySoft = Color(0xFFFFF3EB);
  static const Color primaryBorder = Color(0xFFFFB273);
  static const Color secondary = Color(0xFF121212);
  static const Color tertiary = Color(0xFF64748B);

  // Theme Colors (Dark Navy/Premium Style)
  static const Color backgroundNavy = Color(0xFF0F172A);
  static const Color surfaceNavy = Color(0xFF1E293B);

  // Theme Colors (Light Theme Style)
  static const Color backgroundLight = Color(0xFFF8FAFC);
  static const Color surfaceLight = Color(0xFFFFFFFF);

  // Accents
  static const Color orangeAccent = Color(0xFFF97316);

  // Neutrals
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color greyText = Color(0xFF64748B); // Slightly darker for light theme legibility
  static const Color borderGrey = Color(0xFFE2E8F0); // Lighter border for light theme
}
